# Write a C++ function that takes a vector of books' pages and a number of students as input and returns the minimum number of pages each student must read. Use binary search to find the optimal allocation.

cpp
